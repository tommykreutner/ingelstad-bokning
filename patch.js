const fs=require("fs");
let c=fs.readFileSync("public/prototype.html","utf-8");
const old = "    })()}\r\n    <div class=\"sec-title\">Program <button class=\"btn btn-secondary btn-sm\" onclick=\"addProgModal()\">+ Nytt</button></div>";
const newCode = "    })()}\r\n    ${(()=>{\r\n      const now=new Date();\r\n      const y=now.getFullYear(),m=now.getMonth()+1;\r\n      const yy=String(m>=8?y:y-1).slice(2);\r\n      const yy2=String(m>=8?y+1:y).slice(2);\r\n      const ht='HT'+yy, vt='VT'+yy2;\r\n      const htChecked=DB.settings.openTerms.includes(ht);\r\n      const vtChecked=DB.settings.openTerms.includes(vt);\r\n      return '<div class=\"sec-title\">Terminer</div>'\r\n        +'<div class=\"card\" style=\"margin-bottom:16px;\">'\r\n        +'<p class=\"text-sm text-muted\" style=\"margin-bottom:12px;\">Kryssa i de terminer som ska vara öppna för bokning. Avkryssning stänger terminen omedelbart.</p>'\r\n        +'<label style=\"display:flex;align-items:center;gap:10px;margin-bottom:10px;cursor:pointer;\"><input type=\"checkbox\" class=\"term-check\" value=\"'+ht+'\" '+(htChecked?'checked':'')+'> <span><strong>'+ht+'</strong> (aug–jan '+yy2+')</span></label>'\r\n        +'<label style=\"display:flex;align-items:center;gap:10px;margin-bottom:16px;cursor:pointer;\"><input type=\"checkbox\" class=\"term-check\" value=\"'+vt+'\" '+(vtChecked?'checked':'')+'> <span><strong>'+vt+'</strong> (jan–jun '+yy2+')</span></label>'\r\n        +'<button class=\"btn btn-primary btn-sm\" onclick=\"saveTermSettings()\">Spara terminsinställningar</button>'\r\n        +'</div>';\r\n    })()}\r\n    <div class=\"sec-title\">Program <button class=\"btn btn-secondary btn-sm\" onclick=\"addProgModal()\">+ Nytt</button></div>";
c=c.replace(old,newCode);
fs.writeFileSync("public/prototype.html",c);
console.log("Done:", c.includes("term-check"));
